extends layout

block content
    h1 Revise Proposal
    if user
      p #{ user.username }
    form(action='/proposals/create',method="POST")
      input(type="hidden",name="precursor",value=precursor)
      include proposal_form.pug
